# dwgmerge 開發教訓紀錄

這份文件記錄開發過程中踩過的坑、根本原因、跟最後的對策，給下次繼續開發（或做類似的 DXF 自動化工具）時參考，避免重蹈覆轍。不是使用者說明文件（那是 `ABOUT.md`），這份是給開發者/AI 助理看的。

## 1. OLE2FRAME（嵌入的 Excel/OLE 物件，例如貼進來的 BOM 表）位置被改了，AutoCAD 卻沒反應

**現象**：用 ezdxf 改了 OLE2FRAME 的座標（group code 10/11），存檔重新讀回來確認座標是對的，但在真正的 AutoCAD 裡開啟，這個物件還是停在原來的位置/大小，完全沒有跟著搬動。

**根本原因**：OLE2FRAME 的位置/大小其實被記錄了「兩份」——一份是一般的 10/11 座標欄位，另一份是**藏在這個實體自己的二進位資料（group code 310，OLE stream 本體）裡的重複座標**（開頭約 98 bytes：2 bytes 的神秘值 + 4 個角點座標，各自是 3 個 little-endian double）。**AutoCAD 讀取畫面顯示位置時，認的是第二份，不是第一份**。只改 10/11，AutoCAD 完全視而不見。

**怎麼確認的**：(1) 直接解析 B 圖自己原始的二進位資料，發現裡面的 4 個角點座標跟 10/11 欄位在原始位置時是完全一致的；(2) 一個只改了 10/11 的版本，經使用者在真正的 AutoCAD 裡測試，畫面上物件位置完全沒變，證實 AutoCAD 認的是二進位那份。

**對策**：新增 `_patch_ole_binary_corners()`，改 10/11 的同時，一定要連同二進位資料裡重複的 4 個角點座標一起同步更新（保留原本的 2-byte 神秘值不動，重新 pack 四個角點、重新切成跟原本一樣大小的 chunk 塞回去）。

**教訓**：DXF 格式裡「看起來對的欄位」不代表 AutoCAD 真的認那個欄位；遇到「改了資料、存檔重讀都對，但真正的 CAD 軟體不認」的狀況，要懷疑是不是有一份隱藏/重複的資料副本沒有同步更新，特別是二進位嵌入資料（OLE、影像等）。

## 2. 「不能重疊」這個規則必須套用到全部東西身上，不能只套用在某一種情境

**現象（第一次）**：BOM、Notes 都被判斷成跟圖框重疊、都搬到圖框外了，但兩個又疊在一起。

**現象（第二次，871203 案例）**：BOM 相關的邏輯（`_relocate_outside_if_overlapping`）只有在 B 圖含 BOM/OLE 物件時才會被呼叫；沒有 BOM 物件的一般零件圖（例如 871203）的 Notes，走的是另一條比較舊、比較弱的邏輯（`_fit_notes_in_frame`）——那個邏輯只處理「Notes 超出圖紙下邊界就往上移」，**完全沒有檢查搬移後是否跟圖框內容（標題欄欄位、A 的表格文字）重疊**，導致 Notes 疊在圖框上。

**根本原因**：同一個「不能重疊」的需求，在程式裡分裂成兩套不同完整度的實作——一套（BOM 案例走的）是後來才做的、比較完整的「檢查重疊→搬到圖框外→跟其他搬出去的東西也不能疊」；另一套（非 BOM 案例走的）是更早、範圍更小的舊邏輯，兩套邏輯沒有同步更新。當使用者說「這個規則」的時候，很容易只想到「當前在討論的那個案例」，但其實這個規則的本質是通用的，不該分兩套。

**對策**：把非 BOM 案例（`elif notes_entity is not None:`）也改成呼叫跟 BOM 案例一樣的 `_relocate_outside_if_overlapping()`，統一成同一套邏輯、同一個 `outside_boxes` 追蹤清單，不再保留 `_fit_notes_in_frame` 這條獨立路徑（函式保留在檔案裡但不再被呼叫，之後可以整個刪掉）。

**教訓**：修一個「規則」的時候，先搜尋整份程式碼，確認這個規則有沒有被實作在多個地方、而且彼此邏輯不同步。如果程式裡對「同一件事」有兩套不同完整度的處理路徑（通常是因為功能是逐步疊代出來的、新需求只改了當時在測試的那條路徑），下次改規則時要問「這個規則理論上該套用在所有分支，還是真的只該套用在這個特定情境？」，不確定就兩邊都改，並各自跑一次回歸測試。

## 3. 「單一位移量」對齊法，只在兩邊圖紙尺寸相近時才成立

**現象**：目標圖紙尺寸（A 的 Layout，例如 A3）跟 B 原本的圖紙尺寸（例如 A4）差很多時，合併後 B 原有的邊框/表格整組留在圖面上、沒被正確替換掉，變成跟 A 的新框重複。

**根本原因**：整個「把 B 的舊物件搬到跟 A 新框對齊、再判斷位置完全重疊的就刪掉舊版」流程，靠的是「算出 A 跟 B 標題欄插入點的差值，整批套用同一個 XY 位移量」。這個方法在兩個框大小相近時剛好會讓邊框對齊、重疊判斷才會命中；但如果目標圖紙比 B 原本的圖紙大/小很多，同一個位移量沒辦法讓兩個不同大小的矩形邊框對齊，「重疊判斷」找不到任何命中，於是「刪除重複舊物件」這一步完全沒有東西可刪，B 的舊邊框/表格就整組留下來了。

**對策**：目前先用「保守擋下」的方式處理——偵測到目標尺寸跟 B 原本尺寸差太多（超過 5mm）就直接丟出清楚的錯誤訊息，請使用者改選跟 B 原本尺寸相符的目標尺寸，不去嘗試做「真正的跨尺寸重新定位」（那需要依 B 框的四個角各自對齊 A 框的對應角，是更大的改動，目前沒有測試案例、風險較高）。

**教訓**：任何「用一個固定的線性變換（位移/縮放）去對齊兩個東西」的邏輯，都要明確意識到它的前提假設是什麼（這裡的前提是「兩個框大小相近」），並且在前提不成立時要主動偵測、擋下並給出清楚原因，而不是讓它默默跑完、產出一個「看起來成功但內容錯誤/重複」的結果——這種結果比直接報錯更危險，因為使用者不會馬上發現。

## 4. 部署環境跟本機開發環境結構不一致，會讓「本機測試都過」失去意義

**現象**：本機驗證過的修正，使用者在真正部署的 Streamlit 網站上測試，卻還是舊的錯誤結果。

**根本原因**：本機開發用的是 `dwgmerge_engine/` 這種子資料夾（package）結構，`app.py` 用 `from dwgmerge_engine import core, merge`；但使用者是透過 GitHub 網頁介面手動一個一個上傳檔案，所有 `.py` 檔案都被放在 repo 根目錄（沒有子資料夾），導致 import 路徑完全對不上。這種結構不一致造成的匯入失敗，有時甚至不會讓網站直接顯示錯誤畫面，而是讓 Streamlit Cloud 卡在serve 舊的、上一次成功部署的版本，看起來像是「更新沒生效」。

**對策**：每次要交付「要上傳到 GitHub 的檔案」時，不能只給本機開發用的資料夾結構，要另外維護/確認一份跟使用者實際 repo 結構（扁平、無子資料夾）一致的版本，並且每次改動後直接對著 `git clone` 下來的真實 repo 做編譯 (`py_compile`) 跟實際執行測試 (`run_merge()`)，不能只信任本機開發資料夾裡測試通過。

**教訓**：「本機測試通過」跟「使用者實際的部署環境測試通過」是兩件不同的事，尤其當使用者的部署方式（手動上傳檔案）跟開發者慣用的方式（git push 整個資料夾結構）不同時，兩邊的檔案結構很容易悄悄產生分歧。有能力的話，直接對真實部署的 repo 做驗證，而不是假設「反正檔案內容一樣就沒問題」。

## 通用心得

1. 使用者回報「還是有問題」時，先假設「你原本以為修好的東西，其實只修好了一部分／只修好了某一種情境」，重新檢查程式碼裡是否有多條路徑處理同一件事，而不是急著猜測是不是又出現全新的 bug。
2. 遇到「改了看起來對的資料、但實際行為沒變」的狀況，要懷疑資料有沒有被重複記錄在別的地方（尤其是二進位/嵌入資料）。
3. 任何依賴「兩邊大小/結構相近」這個隱藏假設的演算法，要主動偵測假設是否成立，假設不成立時清楚報錯，不要讓它默默產出錯認但看起來合理的結果。
4. 交付「要放到某個外部環境（例如使用者手動維護的 GitHub repo）」的檔案時，要對著那個環境實際的結構驗證過，不能只用本機開發資料夾的結構去類推。
