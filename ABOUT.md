## 這個工具做什麼

把舊圖 (**B**) 合併進公司標準圖框 (**A**)：換上 A 的標題欄/圖框、把 B 的實際內容（型號、圖名、材質…）填回新欄位、依圖框尺寸自動算出一個乾淨的整數比例（1:2、1:4、2:1…，不會出現 1:3.34 這種除不盡的數字）、可選擇三種合併模式，並把運算過程中發現的重疊、找不到對應值等狀況列出來給你確認。

## 目前的限制（請仔細看）

**只吃 DXF，不吃 DWG。** 免費的 ODA File Converter 授權條款不允許「自動化/伺服器批次」使用，這個工具目前又沒有預算買商用轉檔 SDK 或雲端 API，所以無法在工具內把 DWG 自動轉成 DXF 或轉回去。目前的作法：

1. 在 AutoCAD 裡把 A 和每一份 B 都「另存新檔 → DXF」。
2. 上傳 DXF 到這個工具。
3. 工具處理完，下載回來的也是 DXF；你自己在 AutoCAD 開啟後「另存新檔 → DWG」。

如果之後公司買了轉檔 API（或用 Autodesk 自己的 Forge/APS），可以再把這段補上，介面上「上傳/輸出」的選項已經預留了擴充空間。

**沒有畫面上的拖曳功能。** 這是純後端處理工具，沒有一個可以在瀏覽器裡「點一下、拖一下」的 CAD 畫布。如果合併後有東西重疊，工具會盡量：(a) 偵測到是「A、B 各自都有一份一樣的邊框/表頭」時，直接刪掉 B 的舊版、留 A 的新版，不會真的重疊；(b) 對於一般的舊物件衝突，工具會先試著在圖紙內找一個不會壓到圖框內容、也不會壓到模型視圖（viewport 顯示範圍）的空位自動挪過去；(c) 如果整張圖紙內都找不到安全的空位，就直接把該物件移到**圖框外側**（圖紙邊界右方），寧可讓你事後手動拖回正確位置，也不冒險把它疊在模型或其他內容中間。其餘沒被判斷成衝突的內容不會自動搬動。

**半成品圖（含 BOM 表格 / OLE 物件）的版面配置**：如果 B 圖含有以 OLE 物件形式嵌入的 BOM 表（例如貼進來的 Excel 表格），工具的規則很單純，只有兩個判斷點，**完全不會自動縮放或改變模型比例**：

1. BOM／OLE 物件：檢查它目前的位置（沿用 B 圖原有位置、依新圖框做相同的平移）有沒有跟圖框內容或模型視圖重疊。沒有重疊就完全不動；有重疊，就整個維持原始大小搬到**圖框外（圖紙右側）**，交給你自己在 AutoCAD 裡拖回合適位置。
2. Notes／備註：同樣的邏輯，檢查有沒有跟圖框內容或模型視圖重疊。沒有就不動；有重疊，就整段搬到圖框外，字級大小完全不變。
3. **圖框外側也一樣不能互相重疊**：如果 BOM 和 Notes 都被搬到圖框外，工具會檢查這兩個（以及其他被搬出去的物件）彼此之間有沒有疊在一起——如果有，就把後放的那個再往下移一段距離（留一點間距），直到跟其他已經搬出去的東西都不重疊為止。

（這個規則背後其實有一個技術上的教訓：嵌入的 Excel/OLE 物件（BOM 表）在 DXF 檔案裡，位置/大小其實被記錄了「兩份」——一份是一般看得到的座標欄位（10/11），另一份是**藏在 OLE 物件自己的二進位資料裡的重複座標**。一開始工具只改了第一份，AutoCAD 開啟時卻是認第二份，所以不管怎麼改座標、搬位置，AutoCAD 畫面上都不會變。這個問題已經找到並修正——現在搬動 BOM/Notes 時，兩份座標會一起同步更新，AutoCAD 開啟後位置一定會正確反映。目前工具刻意只做「搬動位置」不做「縮放大小」，是配合你的需求簡化成最單純可靠的規則，不是技術上做不到。）

沒有 BOM／OLE 物件的一般零件圖（例如 871203 這種案例）維持原本邏輯：Notes 放不下時只搬動位置、不縮小字級。

**Notes / 備註的結合方式**：工具會自動讀出 A 標準備註與 B 舊圖備註的每一條項目，逐條比對用詞是否相似——如果 B 的某一項跟 A 已有的項目說的是同一件事，就捨棄 B 的版本、以 A 的用詞為準；B 真正新增的項目（或你另外手動輸入的備註）則接續在 A 的項目編號之後加進去。用詞相似度處於中間值、無法確定是否重複時，仍會保留該項目並在警告訊息中列出，請自行確認是否重複。字級/字型本身則跟隨你選擇的合併模式（模式1、3沿用 A 的字級，模式2 改用 B 原本的字級），縮小規則見上一段。

**三種合併模式的完成度不同：**
- **模式 1**（B 保留內容，其餘全部套用 A 的格式）：這是原本手動驗證過、最完整測試的路徑。
- **模式 2 / 模式 3**：邏輯上已經實作（換框、依模式決定要不要套用 A 的顏色/字型/線寬），但因為每份圖面的舊格式千變萬化，"完全比照 B 的格式" 這種要求無法做到 100% 通用判斷，複雜的情況請合併後在 AutoCAD 打開來確認一次。

**標題欄比對邏輯**：假設 A、B 是「同一個系列的模板」（例如都衍生自同一份 Wellell 標準），用「屬性名稱 (tag) + 在圖框內的相對位置」來配對新舊值；如果找不到位置相同的，退而求其次只比對屬性名稱。如果哪天公司要導入一份「完全長得不一樣」的新模板，這個配對邏輯可能配不準，需要人工檢查一次結果。

## 三種合併模式在做什麼

| 模式 | 內容 (幾何/欄位值) | 格式 (顏色/字型/線寬) | 圖框/標題欄圖案 |
|---|---|---|---|
| 1 | 用 B 的 | 全部改成 A 的標準 | 用 A 的 |
| 2 | 用 B 的 | 盡量維持 B 原本的 | 用 A 的（但如果 A 圖框本身文字格式跟 B 衝突，以 B 為主） |
| 3 | 用 B 的 | A 的部分維持 A 原樣、B 的部分維持 B 原樣，兩邊都不改 | 用 A 的 |

## 比例欄位規則

- 一般零件：一定要填一個乾淨的整數比例（1:1, 1:2, 1:2.5, 1:3, 1:4, 1:5, 1:10, 1:15, 1:20, 1:25, 1:50, 1:100…，或反過來的 2:1、5:1 等放大比例），工具會自動挑「在圖框可視範圍內放得下的最大比例」。
- 半成品/組裝件：勾選「半成品」後，比例欄固定填 n/a，但工具還是會把模型縮放到剛好放得下圖框（只是不把這個縮放值當成正式比例寫進欄位）。

## 資料存放

基準圖框 (A) 存放在這個工具自己的檔案系統裡；如果有設定 GitHub 同步（見 README「部署」章節），更新 A 也會同步推一份到 GitHub repo，之後不管誰重新部署或誰打開這個工具，看到的都是同一份最新的 A。批次上傳的 B 圖面跟合併結果**不會**被保存 —— 你下載下來的 ZIP 是唯一的副本，工具本身不留檔。
